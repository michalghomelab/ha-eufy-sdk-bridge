{
  "name": "ha-eufy-sdk-bridge",
  "version": "0.2.0-ptz.9",
  "private": true,
  "type": "module",
  "description": "WebSocket + HTTP bridge exposing the eufy-sdk SDK to a frontend (Home Assistant, a web UI, anything), with go2rtc for live video.",
  "main": "server.mjs",
  "bin": {
    "eufy-sdk-bridge": "bin/start.sh"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs",
    "lint": "prettier --check .",
    "format": "prettier --write ."
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "@mega-yfue/eufy-sdk": "github:michalghomelab/eufy-sdk#main",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
